"use client";

import Tickets from "@/components/myComponents/Tickets";

export default function TicketsPage() {
  // You can use params.id if you need to access a specific ticket ID
  // For example, if you want to fetch ticket details based on the ID:
  // const ticketId = params.id;
  return <Tickets />;
}
